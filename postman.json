{
  "info": {
    "_postman_id": "abc123",
    "name": "Express Cart API",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Shop Item Routes",
      "item": [
        {
          "name": "Get all shop items",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/admin/shop-items"
          }
        },
        {
          "name": "Add a new shop item",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/admin/shop-items"
          }
        },
        {
          "name": "Get a shop item by ID",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/admin/shop-items/:id"
          }
        },
        {
          "name": "Update a shop item by ID",
          "request": {
            "method": "PATCH",
            "url": "{{base_url}}/admin/shop-items/:id"
          }
        },
        {
          "name": "Delete a shop item by ID",
          "request": {
            "method": "DELETE",
            "url": "{{base_url}}/admin/shop-items/:id"
          }
        },
        {
          "name": "Search for shop items",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/admin/shop-items/search"
          }
        },
        {
          "name": "Filter shop items",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/admin/shop-items/filter"
          }
        }
      ]
    },
    {
      "name": "Customer Routes",
      "item": [
        {
          "name": "Get all shop items for customers",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/customer/shop-items"
          }
        },
        {
          "name": "Get a shop item by ID for customers",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/customer/shop-items/:id"
          }
        },
        {
          "name": "Search for shop items for customers",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/customer/shop-items/search"
          }
        },
        {
          "name": "Filter shop items for customers",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/customer/shop-items/filter"
          }
        },
        {
          "name": "Get the customer's cart",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/customer/cart"
          }
        },
        {
          "name": "Add an item to the customer's cart",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/customer/cart"
          }
        },
        {
          "name": "Update an item in the customer's cart",
          "request": {
            "method": "PUT",
            "url": "{{base_url}}/customer/cart"
          }
        },
        {
          "name": "Remove an item from the customer's cart",
          "request": {
            "method": "DELETE",
            "url": "{{base_url}}/customer/cart"
          }
        },
        {
          "name": "Checkout",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/customer/checkout"
          }
        }
      ]
    },
    {
      "name": "Authentication Routes",
      "item": [
        {
          "name": "Admin Registration",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/admin/register"
          }
        },
        {
          "name": "Admin Login",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/admin/login"
          }
        },
        {
          "name": "Admin Logout",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/admin/logout"
          }
        },
        {
          "name": "Customer Registration",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/customer/register"
          }
        },
        {
          "name": "Customer Login",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/customer/login"
          }
        },
        {
          "name": "Customer Logout",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/customer/logout"
          }
        }
      ]
    },
    {
      "name": "Authorization Routes",
      "item": [
        {
          "name": "Middleware for authorization checks",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/auth/middleware"
          }
        }
      ]
    },
    {
      "name": "Admin Functionalities",
      "item": [
        {
          "name": "Get all orders",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/admin/orders"
          }
        },
        {
          "name": "Get all customers",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/admin/customers"
          }
        },
        {
          "name": "Create a new admin",
          "request": {
            "method": "POST",
            "url": "{{base_url}}/admin/new-admin"
          }
        }
      ]
    },
    {
      "name": "Customer Functionalities",
      "item": [
        {
          "name": "Get all orders for customer",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/customer/orders"
          }
        },
        {
          "name": "Get customer profile",
          "request": {
            "method": "GET",
            "url": "{{base_url}}/customer/profile"
          }
        },
        {
          "name": "Update customer profile",
          "request": {
            "method": "PUT",
            "url": "{{base_url}}/customer/profile"
          }
        }
      ]
    }
  ],
  "variable": [
    {
      "key": "base_url",
      "value": "http://localhost:3000",
      "type": "string"
    }
  ]
}
